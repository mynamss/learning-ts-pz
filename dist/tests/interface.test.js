describe('Interface in TS', () => {
    it('should same output with Any', () => {
        const president = {
            id: 1,
            name: "Joko Widodo"
        };
        console.info(president);
    });
});
export {};
