describe('Data Type', () => {
    it('should declare', () => {
        // declare data type
        let name: string = "Maul"
        let balance: number = 100000
        let isVip: boolean = true
        
        // show in console
        console.info(name)
        console.info(balance)
        console.info(isVip)

        // change data types = error
        // name = 1
        // balance = "1"
        // isVip = 1
    });
});